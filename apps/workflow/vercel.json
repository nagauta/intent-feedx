{
  "buildCommand": "cd ../.. && bun run turbo build --filter=@intent-feedx/workflow",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "cd ../.. && bun install",
  "crons": [
    {
      "path": "/api/cron/screenshot",
      "schedule": "0 * * * *"
    }
  ]
}
