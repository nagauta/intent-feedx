{
  "name": "@intent-feedx/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "search": "bun run src/search.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "dayjs": "^1.11.19",
    "hono": "^4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
